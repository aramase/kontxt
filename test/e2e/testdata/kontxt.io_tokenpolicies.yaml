---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: tokenpolicies.kontxt.io
spec:
  group: kontxt.io
  names:
    kind: TokenPolicy
    listKind: TokenPolicyList
    plural: tokenpolicies
    shortNames:
    - tp
    singular: tokenpolicy
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          TokenPolicy defines security guardrails for transaction tokens.
          Owned by the security admin.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TokenPolicySpec defines the policy constraints.
            properties:
              accessEvaluationWebhook:
                description: |-
                  AccessEvaluationWebhook is an optional external webhook for policy decisions
                  that CEL cannot express.
                properties:
                  enabled:
                    description: Enabled controls whether the webhook is active.
                    type: boolean
                  endpoint:
                    description: Endpoint is the URL of the webhook.
                    type: string
                  failurePolicy:
                    default: Deny
                    description: FailurePolicy determines behavior on webhook failure.
                    enum:
                    - Deny
                    - Allow
                    type: string
                required:
                - enabled
                type: object
              authorizedRequesters:
                description: AuthorizedRequesters restricts which service accounts
                  can request TxTokens.
                items:
                  description: RequesterRule defines which service accounts can request
                    TxTokens.
                  properties:
                    namespaceSelector:
                      description: NamespaceSelector selects namespaces for this rule.
                      properties:
                        matchExpressions:
                          description: matchExpressions is a list of label selector
                            requirements. The requirements are ANDed.
                          items:
                            description: |-
                              A label selector requirement is a selector that contains values, a key, and an operator that
                              relates the key and values.
                            properties:
                              key:
                                description: key is the label key that the selector
                                  applies to.
                                type: string
                              operator:
                                description: |-
                                  operator represents a key's relationship to a set of values.
                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                type: string
                              values:
                                description: |-
                                  values is an array of string values. If the operator is In or NotIn,
                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                  the values array must be empty. This array is replaced during a strategic
                                  merge patch.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: atomic
                            required:
                            - key
                            - operator
                            type: object
                          type: array
                          x-kubernetes-list-type: atomic
                        matchLabels:
                          additionalProperties:
                            type: string
                          description: |-
                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                          type: object
                      type: object
                      x-kubernetes-map-type: atomic
                    serviceAccountNames:
                      description: |-
                        ServiceAccountNames is a list of allowed service account names.
                        Supports wildcards (e.g., "agent-*").
                      items:
                        type: string
                      type: array
                  type: object
                type: array
              authorizedTransactionNamespaces:
                description: AuthorizedTransactionNamespaces restricts which namespaces
                  can define TransactionTypes.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              constraints:
                description: Constraints define ceilings that TransactionTypes cannot
                  exceed.
                properties:
                  disallowedEnrichers:
                    description: DisallowedEnrichers blocks specific enricher plugins.
                    items:
                      type: string
                    type: array
                  mandatoryRctxFields:
                    description: MandatoryRctxFields are rctx fields required in ALL
                      tokens.
                    items:
                      type: string
                    type: array
                  mandatoryTctxFields:
                    description: MandatoryTctxFields are tctx fields required in ALL
                      tokens.
                    items:
                      type: string
                    type: array
                  maxTokenLifetime:
                    description: MaxTokenLifetime is the maximum allowed token lifetime.
                    type: string
                type: object
              issuanceRules:
                description: |-
                  IssuanceRules are CEL expressions evaluated by the TTS before issuing a TxToken.
                  All rules must evaluate to true for the token to be issued.
                items:
                  description: IssuanceRule is a CEL expression evaluated before issuing
                    a TxToken.
                  properties:
                    cel:
                      description: |-
                        CEL is a CEL expression that must evaluate to true.
                        Available variables: subject, scope, tctx, rctx, workload, namespace.
                      type: string
                    message:
                      description: Message is the error message when the rule fails.
                      type: string
                    name:
                      description: Name identifies this rule.
                      type: string
                  required:
                  - cel
                  - message
                  - name
                  type: object
                type: array
              targetNamespaces:
                description: |-
                  TargetNamespaces limits this policy to specific namespaces.
                  If empty, the policy applies to all namespaces.
                properties:
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: MatchLabels selects namespaces by label.
                    type: object
                  matchNames:
                    description: MatchNames selects namespaces by exact name.
                    items:
                      type: string
                    type: array
                type: object
            type: object
          status:
            description: TokenPolicyStatus defines the observed state of TokenPolicy.
            properties:
              conditions:
                description: Conditions represent the latest available observations.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
