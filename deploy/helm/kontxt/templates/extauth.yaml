apiVersion: v1
kind: ServiceAccount
metadata:
  name: kontxt-extauth
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: kontxt-extauth
    app.kubernetes.io/part-of: kontxt
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kontxt-extauth
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: kontxt-extauth
    app.kubernetes.io/part-of: kontxt
spec:
  replicas: {{ .Values.extauth.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: kontxt-extauth
  template:
    metadata:
      labels:
        app.kubernetes.io/name: kontxt-extauth
        app.kubernetes.io/part-of: kontxt
    spec:
      serviceAccountName: kontxt-extauth
      containers:
        - name: extauth
          image: "{{ .Values.extauth.image.repository }}:{{ .Values.extauth.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.extauth.image.pullPolicy }}
          args:
            - --addr=:{{ .Values.extauth.port }}
            - --health-addr=:{{ .Values.extauth.healthPort }}
            - --tts=http://kontxt-tts.{{ .Values.namespace }}.svc.cluster.local
            - --jwks=http://kontxt-tts.{{ .Values.namespace }}.svc.cluster.local/.well-known/jwks.json
            - --trust-domain={{ .Values.tts.config.trustDomain }}
            - --mode=verify
            - --controller-addr=kontxt-controller.{{ .Values.namespace }}.svc.cluster.local:9090
          ports:
            - containerPort: {{ .Values.extauth.port }}
              name: grpc
              protocol: TCP
            - containerPort: {{ .Values.extauth.healthPort }}
              name: health
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /healthz
              port: health
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: health
            initialDelaySeconds: 1
            periodSeconds: 2
          resources:
            {{- toYaml .Values.extauth.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: kontxt-extauth
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: kontxt-extauth
    app.kubernetes.io/part-of: kontxt
spec:
  selector:
    app.kubernetes.io/name: kontxt-extauth
  ports:
    - port: 9000
      targetPort: grpc
      protocol: TCP
      name: grpc
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kontxt-extauth-generate
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: kontxt-extauth-generate
    app.kubernetes.io/part-of: kontxt
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kontxt-extauth-generate
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: kontxt-extauth-generate
    app.kubernetes.io/part-of: kontxt
spec:
  replicas: {{ .Values.extauth.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: kontxt-extauth-generate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: kontxt-extauth-generate
        app.kubernetes.io/part-of: kontxt
    spec:
      serviceAccountName: kontxt-extauth-generate
      containers:
        - name: extauth
          image: "{{ .Values.extauth.image.repository }}:{{ .Values.extauth.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.extauth.image.pullPolicy }}
          args:
            - --addr=:{{ .Values.extauth.port }}
            - --health-addr=:{{ .Values.extauth.healthPort }}
            - --tts=http://kontxt-tts.{{ .Values.namespace }}.svc.cluster.local
            - --jwks=http://kontxt-tts.{{ .Values.namespace }}.svc.cluster.local/.well-known/jwks.json
            - --trust-domain={{ .Values.tts.config.trustDomain }}
            - --mode=generate
            - --controller-addr=kontxt-controller.{{ .Values.namespace }}.svc.cluster.local:9090
          ports:
            - containerPort: {{ .Values.extauth.port }}
              name: grpc
              protocol: TCP
            - containerPort: {{ .Values.extauth.healthPort }}
              name: health
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /healthz
              port: health
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: health
            initialDelaySeconds: 1
            periodSeconds: 2
          resources:
            {{- toYaml .Values.extauth.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: kontxt-extauth-generate
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: kontxt-extauth-generate
    app.kubernetes.io/part-of: kontxt
spec:
  selector:
    app.kubernetes.io/name: kontxt-extauth-generate
  ports:
    - port: 9000
      targetPort: grpc
      protocol: TCP
      name: grpc
